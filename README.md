# Starlight Trader
A real-time trading platform with automated risk management.

## Getting Started
1. Initialize the project: `go mod init starlight-trader`
2. Install dependencies: `go get -u github.com/gorilla/mux`
3. Run the application: `go run src/main.go`